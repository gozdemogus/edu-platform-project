@model List<Category>

<section class="ftco-section">
    <div class="container">
        <div class="row justify-content-center mb-5">
            <div class="col-md-7 heading-section text-center ftco-animate">
                <span class="subheading">Course Categories</span>
                <h2 class="mb-0">Top Categories</h2>
            </div>
        </div>
        @*<div class="row">
                <div class="col-md-3 ftco-animate">
                    <ul class="category text-center">
                        @foreach (var item in Model)
                        {
                            <li><a href="#">@item.CategoryName<br><span class="number">354</span> <span>Open position</span><i class="ion-ios-arrow-forward"></i></a></li>
                        }
                    </ul>
                </div>
            </div>*@

        <div class="row">
            <div class="col-md-4 ftco-animate">
                <ul class="category text-center">
                    @foreach (var item in Model)
                    {
                        <li><a href="/Course/ByCategory/@item.CategoryID">@item.CategoryName <br><span class="number">@item.Courses.Count</span> <span>@item.Description</span><i class="ion-ios-arrow-forward"></i></a></li>

                    }

                </ul>
            </div>
            <div class="col-md-4 ftco-animate">
                <ul class="category text-center">
                    @foreach (var item in ViewBag.c2)
                    {
                        <li><a href="/Course/ByCategory/@item.CategoryID">@item.CategoryName <br><span class="number">@item.Courses.Count</span> <span>@item.Description</span><i class="ion-ios-arrow-forward"></i></a></li>

                    }

                </ul>
            </div>
            <div class="col-md-4 ftco-animate">
                <ul class="category text-center">
                    @foreach (var item in ViewBag.c3)
                    {
                        <li><a href="/Course/ByCategory/@item.CategoryID">@item.CategoryName <br><span class="number">@item.Courses.Count</span> <span>@item.Description</span><i class="ion-ios-arrow-forward"></i></a></li>

                    }

                </ul>
            </div>

        </div>
    </div>
</section>
